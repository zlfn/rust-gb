[package]
name = "gb-bank-pack"
version.workspace = true
edition.workspace = true
authors.workspace = true
description = "Link-time bank allocator for the gb-bank toolchain"

[dependencies]
gb-header-fix = { path = "../gb-header-fix" }
object = "0.36"
rustc-demangle = "0.1"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
