[package]
name = "gb-header-fix"
version.workspace = true
edition.workspace = true
authors.workspace = true
description = "Game Boy ROM header configuration and patching"

[dependencies]
toml = "0.8"
serde = { version = "1", features = ["derive"] }
