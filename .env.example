# Copy this file to `.env` and set the path for your machine.
#
# Path to the llvm-z80 build `bin` directory, which holds the SM83 toolchain
# (clang, ld.lld, llvm-nm, llvm-ar, llvm-objcopy). Used by the build.
LLVM_Z80_BIN=/path/to/llvm-z80/build/bin
